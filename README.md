# Where's the Boss Website
## Setup
- run `npm install` or `yarn install` to install dependencies
- run `npm run build` or `yarn run build` to compile `tailwind.css`
- open `dist/index.html` in a web browser